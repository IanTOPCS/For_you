start:
  ;搬入要家的值至指定連續位置
	mov 0x50, #0x03
	mov 0x51, #0x25
	mov 0x52, #0x17
	mov 0x53, #0x06
	
  ;使用 累加器 將所有值累加(考慮進位故非ADD，是ADDC)
	clr a
	addc a, 0x50
	addc a, 0x51
	addc a, 0x52
	addc a, 0x53

  ;將結果搬至指定位置
	mov 0x60, a
	jmp $